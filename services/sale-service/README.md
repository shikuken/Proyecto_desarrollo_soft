Sale service

Run:

1. cd services/sale-service
2. npm install
3. npm run start

Env vars:
PORT (default 4003)
DB_PATH (default ./data/sales.sqlite)
PRODUCT_SERVICE_URL (e.g. http://localhost:4001)
CLIENT_SERVICE_URL (e.g. http://localhost:4002)
